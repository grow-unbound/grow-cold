import type { TextStyle } from 'react-native';

/** Bottom tab label style (single source; avoid inline literals in navigators). */
export const rootTabBarLabelStyle: TextStyle = {
  fontSize: 11,
  fontWeight: '600',
};
